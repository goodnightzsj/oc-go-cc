# 上游调查与安全测试基线

- Shape: single-full。
- 目标：确认 origin/upstream 当前分支与差异，评估可采纳修复；让现有测试只访问临时 HOME/DB，消除已证实的时间/网络依赖。
- 非目标：不整树覆盖，不修改生产状态，不先升级无关依赖。
- 环境：Go 1.26.4，本项目 go.mod 1.25；Go test/race，内嵌 JS 无新框架。
- 验收：原有测试基线可重复，上游采纳表包含提交 SHA、价值、冲突与处理结论。
- 验证：针对性 go test，再由最终任务运行全量 race/vet/build。
