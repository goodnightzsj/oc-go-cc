# 接入文档与最终验证

## Task Shape

single-full；依赖04-dashboard。

## Goals

- 提供CommandCode独立配置及Claude Code/Codex示例，明确原生与适配路径、工具/流式支持及不支持的Responses能力。
- 更新README和API文档，保存官方依据、参考项目固定提交与上游语义移植记录。
- 全量race、vet、六目标无CGO编译与面板冒烟；真实平台、其他系统运行和原生托盘未实测不宣称兼容。
- 2026-09-11 新增平台查询与套餐接口重新验证；五平台逐页覆盖，文档区分官方账户数据与本地流量，未完成项不得结项。

## Constraints

不修改用户全局配置，不读取真实密钥，不整树合并上游，不改动发布流水线；本子任务完成后交给 #6 执行已授权的部署。

## Done-When

文档示例和实际配置/协议一致；全部子任务通过验证并更新任务真源；记录剩余边界。

## Validation

隔离环境`go test -race ./...`、`go vet ./...`、六目标构建、文档示例校验和`git diff --check`。
