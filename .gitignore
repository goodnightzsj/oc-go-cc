bin/
dist/
*.exe
*.pid
.env
# Real proxy config holds API keys; only the shipped example is tracked.
configs/*.json
!configs/config.example.json
.tmp/
oc-go-cc
/routatic-proxy
/routatic-hosted
.trunk
dist/
brag-output-**
.DS_Store
.kimchi
.worktrees/
.planning
# Spartan AI config (contains API keys)
.spartan/ai.env
.claude

*.test
.playwright-mcp
.gstack/
node_modules/

# Generated Windows resource files (built by go-winres in CI)
*.syso
__pycache__/
*.pyc

# Tool scratch directories created in the working tree
.llmdoc-tmp/
artifacts/

# Local working records. These stay on disk for the CLIs that read them but
# are not part of the published tree.
.codex-tasks/
.ai/

# Local-only tooling and operational scripts. Impeccable is still used in this
# checkout; the context file just does not belong to the published tree, and
# platform_reconcile.py can write to the production database, so it stays a
# local reference rather than a shipped script.
.impeccable.md
scripts/platform_reconcile.py

# One-time oc-go-cc -> routatic-proxy merge planning; the merge completed
# 2026-08-06 (f4616cc), so this only survives as the author's local copy.
MIGRATION.md

# Stray file from a shell quoting accident. The real fixtures live under
# internal/*/testdata/.
/testdata/

# Runtime databases hold real usage records and must never be committed.
# The deployed DB lives in ~/.local/share/routatic-proxy/, so these only
# catch a local run pointed at a relative path.
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
