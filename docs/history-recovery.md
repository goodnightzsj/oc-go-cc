# OpenCode 历史恢复与保留策略

## 关闭自动清理

`storage.retention_days` 为负数（推荐 `-1`）时禁用请求历史清理；省略或设置为 `0` 仍使用默认 7 天，正整数为保留天数。仅修改该字段，保留其他配置；重启后以日志 `request retention cleanup disabled` 确认生效。此设置不删除或更改已有记录。

实现入口为 `internal/storage/retention.go`。旧版本把所有非正数当作 7 天，必须先部署包含禁用修复的版本再恢复历史。恢复后不要回滚到仍把负数当成 7 天的旧程序，否则启动清理可能再次删除历史。

## 已核实的最终同步方法

来源为本项目 Claude session `8ebffb0f-fb58-4290-b33e-b8345253d709.jsonl` 的 2026-09-04 11:19Z 完成记录（3062 行）及 14:25Z 文档更正（4450 行）；8 月旧方案不是最终方案。

- 在已有 Edge 登录态中获取 `usage.list` 的实际协议，逐页抓到明确空页，检查分页顺序、重复 ID 和首尾时间。发生请求错误或解析失败必须停止，不能把错误当成分页终点。
- 原始 `inputTokens` 与 `cacheReadTokens` 分开保存，不使用页面四舍五入后的合计数。缓存写入、推理等字段按实际响应保留；缺失字段不能凭空补造。
- `cost` 的整数单位为 `1e-8 USD`，写入费用时直接除以 `1e8`，不先舍入到 6 位，也不再乘套餐倍率。
- 官方 `timeCreated` 是完成时刻，本地 `start_time` 是开始时刻；匹配需核对模型、Token 和实际完成时间。仅金额相等不足以证明同一请求。
- 官方导入行使用稳定官方 ID，归属 `opencode-go`，费用来源 `provider`。无法从备份唯一证实的状态、耗时、流式和路由信息应标记未知（`details_known=0`），不能伪装成成功请求。

历史 `oc_sync.py` 存在非事务 `INSERT OR REPLACE`、费用舍入和默认成功等行为；`scripts/platform_reconcile.py` 还有金额配对及固定模型写入（该脚本已移出跟踪，仅在本机保留）。这些脚本可帮助理解旧协议，不应直接运行其写入模式恢复生产。

## 恢复与验证顺序

1. 核对服务实际数据库路径，独立备份当前配置与 SQLite，验证备份完整性；不要用旧库整体覆盖现库。
2. 对比官方最新导出、旧快照和全部相关备份。较新的备份也可能包含旧的错误 Token 口径或中间重复记录；保留原文件，先在副本演练。
3. 只恢复可证明的缺失记录，以唯一 ID 幂等导入，遇到歧义保留报告。使用事务和参数化 SQL，不删除未匹配的其他平台或当前请求。
4. 对账记录数、模型、纯输入、缓存读取/写入、输出、整数费用与时间；二次执行应零新增。验证真实历史接口和页面，不以健康检查代替数据验收。

## 2026-09-12 早期取证状态

现存官方旧快照有 6561 个唯一 ID，覆盖 2026-08-06 06:56:37Z 至 2026-09-04 11:39:44Z，费用合计 2514740738 单位（$25.14740738）。当前 `provider_usage` 的 1390 条原始账单逐项全部包含在该快照中。

该 TSV 没有保留缓存写入字段，也不是当前全量导出。9 月 4 日早期全量数据库的 6365 行存在未拆缓存的旧口径，不能直接还原。当前是否完成恢复、部署和浏览器验证，以 `.codex-tasks/20260910-platform-integration/tasks/13-history-restore/` 的任务记录为准。

## 2026-09-12 实际恢复结果（部分）

- 远端运行 `d7b25fb`，`storage.retention_days=-1`，启动日志确认清理已禁用。
- 第一批从现存 `provider_usage` 精确恢复 1390 条，官方导入行 `details_known=0`，不把未知状态/耗时显示为实际成功请求。
- 第二批核对 9 月 4 日两份备份的 2340 条原生请求：2259 条与旧官方明细按模型、纯输入、输出、缓存读取和完成秒一一对应；81 条无精确匹配，未写入。
- 第二批保留备份的原始请求 ID、Token 拆分、状态、耗时和费用来源。按 `1e-8 USD` 单位比较，2256 条存储金额与旧官方费用相同；另有 2 条 `estimated` 差异和 1 条旧 `provider` 标记差异（低 7 单位，即 $0.00000007）。这些行保留备份原值及原标记，待当前官方全量对账，未擅自重算或提升可信度。没有使用错误的全量输入旧导入行。
- 两批合计 **3649 条**；纯输入 15257701、输出 2228131、缓存读取 932367237、缓存写入 0。逐页历史 API 的唯一 ID、Token 与汇总一致；副本及生产重复执行均零新增，冲突会拒绝写入，其他表没有变化。

本轮独立数据库及配置备份位于远端 `/root/oc-go-cc/.tmp/native-history-audit-20260912.MQ4rmh/`，目录 0700，文件 0600；不得提交备份或原始账单到仓库。恢复来源与验证摘要见本地任务记录 `../.codex-tasks/20260910-platform-integration/tasks/13-history-restore/raw/native-restoration-result.json`（该目录不随仓库分发）。

以上是当时的部分恢复记录，已由下述全量对账补齐。保留早期结果用于追溯，不要再次运行旧清表/覆盖脚本。

## 2026-09-13 全量恢复与生产验收

原 Edge 连接恢复后，重新从官方已登录用量页获取 **6561 个唯一 ID**：132 个非空页之后到达明确空页，抓取前后首页稳定。当前官方可见范围为 2026-08-06 06:56:37Z 至 2026-09-04 11:39:44Z；不代表官方未返回的后续日期也有请求。

全量字段保留后，在新独立备份和隔离演练通过的前提下，事务新增 2912 行、校正既有 2259 行的费用/来源/可信度；没有覆盖原始请求详情或其他平台数据。最终 `requests` 与 `provider_usage` 各有 6561 条，纯输入 42921501、输出 5069315、缓存读取 1518758021、缓存写入 0，费用精确合计 **$25.14740738**。

- 2337 条保留可唯一对应的原生请求详情；4224 条详情未知，不虚构成功、延迟或流式状态。
- 剩余 3 条备份日志没有唯一官方对应，原备份仍保留，不额外插入造成重复计费。
- 重复执行零新增/更新；冲突回滚、其他表无差异及 SQLite 完整性检查通过。
- 原 Edge 全分页、历史汇总、分析汇总与 SQLite/官方四类 Token 及费用逐项相等；实际历史页显示 6,561 条，未知详情正确展示。
- 正式服务 `d7b25fb` 本次启动仍确认清理禁用，`retention_days=-1`。本轮验收没有再次恢复数据库或发布二进制。

最终备份与演练记录留在远端受限目录 `/root/oc-go-cc/.tmp/official-history-20260912.c9MOtJ/`；本地任务记录保留事务恢复摘要 `../.codex-tasks/20260910-platform-integration/tasks/13-history-restore/raw/official-restoration-result.json` 和生产验收摘要 `../.codex-tasks/20260910-platform-integration/tasks/13-history-restore/raw/production-acceptance.json`（该目录不随仓库分发），不提交原始账单或配置备份。
